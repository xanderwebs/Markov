%h1 Videos

- if @nodes.any?
  %table
    %tr
      %th Name
      %th Views
      %th Recommendations
    - @nodes.each do |node|
      %tr
        %td= node.name
        %td= node.total_visits
        %td= link_to "Recs", recommendations_path(id: node.id, format: :json)
